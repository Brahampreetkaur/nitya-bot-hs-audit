# Copy this file to .streamlit/secrets.toml for local use (that file is gitignored),
# or paste the same content into your app's Secrets when deploying on
# Streamlit Community Cloud (Settings -> Secrets).

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
